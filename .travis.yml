language: node_js
node_js:
  - "12"
  - "11"
  - "10"
dist: xenial

before_install:
  - echo -e "machine github.com\n  login $GITHUB_TOKEN" > ~/.netrc

install:
  - cd app/
  - yarn add electron mocha --save-dev
  - yarn add fs d3 jquery string-pixel-width d3-save-svg jsonpickle plotly.js-dist --save

before_script:

script:
  - cd test/
  - npm test
